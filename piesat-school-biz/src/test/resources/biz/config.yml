smartwork:
  properties:
    common:
      environment: ${spring.profiles.active}
  async:
    default:
      core-pool-size: 20
      max-pool-size: 40
      keep-alive-seconds: 1000
      queue-capacity: 50
